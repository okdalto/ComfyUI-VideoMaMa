# VideoMaMa ComfyUI Custom Node Requirements

# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.23.0
Pillow>=9.0.0

# Diffusers and Transformers
diffusers>=0.24.0
transformers>=4.30.0
accelerate>=0.20.0

# Model downloading
huggingface_hub>=0.19.0

# Additional utilities
opencv-python>=4.8.0
tqdm>=4.65.0
einops>=0.6.0

# Optional: SAM2 for mask generation
# Install separately: git clone https://github.com/facebookresearch/sam2 && cd sam2 && pip install -e .
